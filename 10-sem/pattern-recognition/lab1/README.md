# Звіт виконання лабораторної роботи 1: Розпізнавання образів

## Структура проекту

```
lab1/
├── lab-1.ipynb                  # Jupyter notebook з реалізацією алгоритму
├── REPORT.tex                   # LaTeX звіт (основний файл)
├── REPORT.pdf                   # Скомпільований PDF звіт
├── compile.sh                   # Скрипт для компіляції LaTeX
├── figures/                     # Папка з графіками
│   ├── fig1_input_points.png
│   ├── fig2_convex_hull.png
│   ├── fig3_perpendiculars.png
│   ├── fig4_square_scaling.png
│   ├── fig5_nested_circles.png
│   ├── fig6_nested_ellipses.png
│   └── fig7_ellipse_properties.png
└── README.md                     # Цей файл
```

## Як скомпілювати звіт

### 1. Експортування графіків (якщо ще не зроблено)

Запустити в Jupyter notebook останню cell:
```python
# Експорт графіків для LaTeX звіту (cell у notebook)
```

Це створить папку `figures/` з 7 png файлами.

### 2. Компіляція LaTeX

**На macOS:**
```bash
brew install texlive
bash compile.sh
```

**На Ubuntu/Debian:**
```bash
sudo apt-get install texlive-full
bash compile.sh
```

**На Windows (PowerShell):**
```powershell
pdflatex -interaction=nonstopmode REPORT.tex
pdflatex -interaction=nonstopmode REPORT.tex
```

**Вручну (будь-яка ОС):**
```bash
pdflatex REPORT.tex
pdflatex REPORT.tex  # другий раз для оновлення TOC
```

Результат: `REPORT.pdf`

## Зміст звіту

1. **Титульна сторінка** — інформація про звіт, студента, дисципліну
2. **Зміст** — автоматично генерується
3. **Мета та завдання** — формулювання 8 кроків алгоритму
4. **Теоретичні основи** — математичне обґрунтування методу
5. **Описання алгоритму** — детальний виклад кожного кроку з графіками
6. **Результати** — таблиці класифікації та статистика
7. **Висновки** — аналіз результатів та оцінка методу
8. **Додатки** — вибрані фрагменти Python коду

## Параметри роботи

| Параметр | Значення |
|----------|----------|
| Основних точок (n_main) | 150 |
| Тестових точок (n_test) | 30 |
| Діапазон x | [-50, 50] |
| Діапазон y | [-22, 22] |
| Seed (для відтворюваності) | 42 |

## Ключові результати

- **Найвіддаленіша відстань**: ≈98.74 одиниці
- **Кількість унікальних еліпсів**: 149
- **Коефіцієнт масштабування**: ≈2.15
- **Середня ймовірність**: 0.5424
- **Діапазон ймовірностей**: [0.0265, 0.9735]

## Студентська інформація

- **Прізвище Ім'я По батькові**: Кіщук Ярослав Ярославович
- **Дисципліна**: Розпізнавання образів
- **Проведена**: Лабораторна робота 1
- **Дата звіту**: 2026

## Використані бібліотеки

- NumPy — обробка даних
- Matplotlib — візуалізація графіків
- LaTeX — формування звіту

## Примітки

- Графіки генеруються автоматично з notebook
- Всі формули написані у LaTeX форматі
- Звіт повністю готовий до друку
- Граничні випадки обробляються в основному коді алгоритму

---

**Версія**: 1.0  
**Остання оновлення**: 2026-03-18
